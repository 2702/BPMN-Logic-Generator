package graph;

public enum Marker {
	//processed - oznacza ze jest sprawdzany w aktualnym przebiegu szukania wzorca
	SPLIT, MERGE, MERGE_ROOT, MERGE_TEMP_ROOT, UNTOUCHED, SPLIT_ROOT, SPLIT_TEMP_ROOT
}
